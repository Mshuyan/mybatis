<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shuyan.mybatis.business.mapper.RegionMapper">

    <select id="getRegionById" parameterType="long" resultType="com.shuyan.mybatis.business.bean.RegionEntity">
        select * from region where id=#{id}
    </select>
</mapper>